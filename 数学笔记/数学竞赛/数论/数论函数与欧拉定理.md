---
title: 数论函数与欧拉定理
date: 2026-06-14
tags:
  - 数学
  - 竞赛
  - 数论
  - 数论函数
  - 欧拉定理
author:
  - wym
aliases:
  - Number Theoretic Function
  - Euler Theorem
  - 欧拉函数
---

# 数论函数与欧拉定理（Number Theoretic Functions & Euler's Theorem）

> [!abstract] 核心定位
> 数论函数是定义在正整数上的函数，反映数的算术结构。欧拉函数、莫比乌斯函数是两类最重要的数论函数。欧拉定理将费马小定理从素数推广到任意模，是 RSA 加密的数学基础；莫比乌斯反演则提供了处理求和与因子关系的强大工具。本笔记建立完整的知识链，与 [[求和式的计算方式]]、[[整除与同余基础]] 紧密关联。

---

## 一、欧拉函数 $\varphi(n)$

### 1.1 定义

> [!important] 欧拉函数（Euler's Totient Function）
> $\varphi(n)$ 表示 $1$ 到 $n$ 中与 $n$ 互素的正整数的个数。即：
> $$\varphi(n) = \#\{ k \in \mathbb{Z} \mid 1 \le k \le n,\; \gcd(k, n) = 1 \}$$

### 1.2 计算公式

> [!important] 乘积公式
> 设 $n = p_1^{\alpha_1} p_2^{\alpha_2} \cdots p_k^{\alpha_k}$ 为 $n$ 的素数幂分解，则：
> $$\varphi(n) = n \prod_{i=1}^{k} \left(1 - \frac{1}{p_i}\right) = \prod_{i=1}^{k} p_i^{\alpha_i - 1}(p_i - 1)$$

**推导思路**：利用容斥原理（包含排除原理），从 $1$ 到 $n$ 中减去所有 $p_i$ 的倍数，加上两两交集的倍数，依此类推。

**特殊值**：
- $\varphi(p) = p - 1$（$p$ 为素数）
- $\varphi(p^k) = p^k - p^{k-1} = p^{k-1}(p - 1)$

### 1.3 欧拉函数的性质

> [!important] 性质一：积性（Multiplicativity）
> 若 $\gcd(m, n) = 1$，则 $\varphi(mn) = \varphi(m) \varphi(n)$。

**证明**：由中国剩余定理，$x$ 与 $mn$ 互素当且仅当 $x$ 分别与 $m$ 和 $n$ 互素，而模 $mn$ 的完全剩余系可一一对应到模 $m$ 与模 $n$ 的笛卡尔积上。

> [!important] 性质二：除数求和恒等式
> $$\sum_{d \mid n} \varphi(d) = n$$

**证明思路**：考虑有理数 $\frac{1}{n}, \frac{2}{n}, \dots, \frac{n}{n}$。将每个约分到最简分数 $\frac{a}{d}$（其中 $\gcd(a, d) = 1$，$d \mid n$），分母为 $d$ 的最简分数恰有 $\varphi(d)$ 个。所有分数的分母 $d$ 遍历 $n$ 的正因子，而分数总数为 $n$。

---

## 二、欧拉定理

### 2.1 定理陈述

> [!important] 欧拉定理（Euler's Theorem）
> 若 $\gcd(a, n) = 1$，则：
> $$a^{\varphi(n)} \equiv 1 \pmod n$$

**证明**：设 $r_1, r_2, \dots, r_{\varphi(n)}$ 是模 $n$ 的缩剩余系（即所有与 $n$ 互素且模 $n$ 两两不同余的数）。考虑 $\{a r_1, a r_2, \dots, a r_{\varphi(n)}\}$，由于 $\gcd(a, n) = 1$，该集合也是模 $n$ 的一个缩剩余系。将两组缩剩余系中的元素分别相乘：
$$a^{\varphi(n)} \prod_{i=1}^{\varphi(n)} r_i \equiv \prod_{i=1}^{\varphi(n)} r_i \pmod n$$
由于每个 $r_i$ 与 $n$ 互素，乘积 $\prod r_i$ 也与 $n$ 互素，可约去，即得欧拉定理。

### 2.2 费马小定理作为特例

当 $n = p$ 为素数时，$\varphi(p) = p - 1$，欧拉定理退化为：
$$a^{p-1} \equiv 1 \pmod p \quad (\gcd(a, p) = 1)$$
这正是费马小定理。

### 2.3 扩展欧拉定理

> [!important] 扩展欧拉定理（处理指数取模）
> 若 $b \ge \varphi(m)$，则：
> $$a^b \equiv a^{b \bmod \varphi(m) + \varphi(m)} \pmod m$$
> 当 $\gcd(a, m) = 1$ 时，可直接用 $a^b \equiv a^{b \bmod \varphi(m)} \pmod m$。

> [!tip] 应用场景
> 当指数 $b$ 极大时（如 $b$ 本身也是幂的形式），扩展欧拉定理可以将指数降到 $\varphi(m)$ 量级，使计算可行。详见例题 2。

---

## 三、RSA 加密原理简介

RSA 是最经典的非对称加密算法，其数学核心就是欧拉定理。

**密钥生成**：
1. 选择两个大素数 $p, q$，计算 $n = pq$，$\varphi(n) = (p-1)(q-1)$
2. 选择 $e$ 满足 $\gcd(e, \varphi(n)) = 1$（即公钥指数）
3. 计算 $d \equiv e^{-1} \pmod{\varphi(n)}$（即私钥指数）

**加密解密**：明文 $m$（$0 \le m < n$）：
- 加密：$c \equiv m^e \pmod n$
- 解密：$m \equiv c^d \pmod n$

**正确性验证**：$c^d \equiv m^{ed} \equiv m^{1 + k\varphi(n)} \equiv m \pmod n$（由欧拉定理 $m^{\varphi(n)} \equiv 1 \pmod n$ 当 $\gcd(m, n) = 1$；即使不互素，由中国剩余定理也可验证）。

---

## 四、莫比乌斯函数 $\mu(n)$

### 4.1 定义

> [!important] 莫比乌斯函数（Möbius Function）
> 设 $n$ 为正整数：
> $$\mu(n) = \begin{cases}
> 1 & \text{若 } n = 1 \\
> (-1)^k & \text{若 } n \text{ 是 } k \text{ 个不同素数的乘积（无平方因子）} \\
> 0 & \text{若 } n \text{ 含有平方因子（即存在素数 } p \text{ 使 } p^2 \mid n\text{）}
> \end{cases}$$

### 4.2 基本性质

> [!important] 除数求和的「开关」性质
> $$\sum_{d \mid n} \mu(d) = \begin{cases}
> 1 & \text{若 } n = 1 \\
> 0 & \text{若 } n > 1
> \end{cases}$$

**证明**：$n > 1$ 时，设 $n$ 有 $k$ 个不同素因子，则 $\sum_{d \mid n} \mu(d) = \sum_{i=0}^{k} \binom{k}{i} (-1)^i = (1 - 1)^k = 0$。

> [!tip] 为什么这很关键？
> 这个「开关」性质使得莫比乌斯函数成为**反演**的核心——它可以将关于因子的求和转化回原函数。

---

## 五、莫比乌斯反演

> [!important] 莫比乌斯反演公式（Möbius Inversion）
> 设 $f, g$ 为定义在正整数上的函数：
> $$g(n) = \sum_{d \mid n} f(d) \quad \Longleftrightarrow \quad f(n) = \sum_{d \mid n} \mu(d) \, g\!\left(\frac{n}{d}\right)$$

**证明**：若 $g(n) = \sum_{d \mid n} f(d)$，则：
$$\sum_{d \mid n} \mu(d) \, g\!\left(\frac{n}{d}\right) = \sum_{d \mid n} \mu(d) \sum_{e \mid (n/d)} f(e) = \sum_{k \mid n} f(k) \sum_{d \mid (n/k)} \mu(d)$$
内层和由 $\mu$ 的开关性质，仅当 $n/k = 1$（即 $k = n$）时为 $1$，其余为 $0$。故整个和等于 $f(n)$。

> [!tip] 经典应用
> 利用 $\sum_{d \mid n} \varphi(d) = n$ 和莫比乌斯反演，可得 $\varphi(n) = \sum_{d \mid n} \mu(d) \cdot \frac{n}{d}$，这提供了欧拉函数的另一种表达式。

---

## 六、积性函数

> [!important] 积性函数（Multiplicative Function）
> 数论函数 $f$ 称为**积性函数**，如果对任意互素的正整数 $a, b$（即 $\gcd(a, b) = 1$），有：
> $$f(ab) = f(a) f(b)$$

**常见的积性函数**：
| 函数 | 定义 | 积性 |
|---|---|---|
| $\varphi(n)$ | 欧拉函数 | ✅ |
| $\mu(n)$ | 莫比乌斯函数 | ✅ |
| $d(n)$ | 正因子的个数 | ✅ |
| $\sigma(n)$ | 正因子之和 | ✅ |
| $\operatorname{Id}(n) = n$ | 恒等函数 | ✅ |

积性函数的重要性质：只需知道其在素数幂处的值，即可确定函数在所有正整数处的值（由算术基本定理）。两个积性函数的**狄利克雷卷积** $(f * g)(n) = \sum_{d \mid n} f(d) g(n/d)$ 也是积性函数。

---

## 七、典型例题

> [!example] 例 1：计算欧拉函数
> 计算 $\varphi(2024)$。
>
> **解**：$2024 = 2^3 \times 11 \times 23$。
> $$\varphi(2024) = 2024 \cdot \left(1 - \frac{1}{2}\right) \cdot \left(1 - \frac{1}{11}\right) \cdot \left(1 - \frac{1}{23}\right) = 2024 \cdot \frac{1}{2} \cdot \frac{10}{11} \cdot \frac{22}{23} = 1012 \cdot \frac{10}{11} \cdot \frac{22}{23}$$
> $$= 920 \cdot \frac{22}{23} = 880$$

> [!example] 例 2：扩展欧拉定理求大指数
> 求 $7^{222} \bmod 10$。
>
> **解**：$\varphi(10) = 4$，$\gcd(7, 10) = 1$。由欧拉定理 $7^4 \equiv 1 \pmod{10}$。$222 = 4 \times 55 + 2$，故 $7^{222} \equiv (7^4)^{55} \cdot 7^2 \equiv 1^{55} \cdot 49 \equiv 9 \pmod{10}$。

> [!example] 例 3：莫比乌斯函数求和
> 计算 $\sum_{d \mid 30} \mu(d)$。
>
> **解**：$30 = 2 \times 3 \times 5$，无平方因子。由 $\mu$ 的开关性质，$\sum_{d \mid 30} \mu(d) = 0$（因为 $30 > 1$）。直接验证：因子为 $1, 2, 3, 5, 6, 10, 15, 30$，对应的 $\mu$ 分别为 $1, -1, -1, -1, 1, 1, 1, -1$，求和确实为 $0$。

> [!example] 例 4：莫比乌斯反演
> 已知 $g(n) = \sum_{d \mid n} f(d)$ 且 $g(1) = 1, g(2) = 3, g(4) = 7, g(8) = 15$，求 $f(8)$。
>
> **解**：由莫比乌斯反演，
> $$f(8) = \sum_{d \mid 8} \mu(d) \, g\!\left(\frac{8}{d}\right)$$
> $8$ 的因子：$d = 1, 2, 4, 8$，$\mu(1)=1, \mu(2)=-1, \mu(4)=0, \mu(8)=0$。故
> $$f(8) = 1 \cdot g(8) + (-1) \cdot g(4) + 0 \cdot g(2) + 0 \cdot g(1) = 15 - 7 = 8$$

> [!example] 例 5：除数求和恒等式检验
> 验证 $\sum_{d \mid 12} \varphi(d) = 12$。
>
> **解**：$12$ 的因子：$1, 2, 3, 4, 6, 12$。$\varphi(1)=1$，$\varphi(2)=1$，$\varphi(3)=2$，$\varphi(4)=2$，$\varphi(6)=\varphi(2)\varphi(3)=2$，$\varphi(12)=\varphi(3)\varphi(4)=4$。求和：$1+1+2+2+2+4=12$。✅

> [!example] 例 6：RSA 类型的同余计算
> 已知 $n = 33$（$p = 3, q = 11$），$\varphi(33) = 20$，公钥 $e = 7$，私钥 $d = 3$（验证：$7 \times 3 = 21 \equiv 1 \pmod{20}$）。明文 $m = 5$，求密文 $c$ 并验证解密。
>
> **解**：加密 $c \equiv 5^7 \pmod{33}$。$5^2=25$，$5^4 \equiv 25^2 = 625 \equiv 625 - 33 \times 18 = 625 - 594 = 31 \pmod{33}$。$5^7 = 5^4 \cdot 5^2 \cdot 5 \equiv 31 \cdot 25 \cdot 5 = 775 \cdot 5 = 3875$。$3875 = 33 \times 117 + 14$，故 $c \equiv 14 \pmod{33}$。解密：$14^3 \equiv 14^2 \cdot 14 \equiv 196 \cdot 14$，$196 \equiv 196 - 33 \times 5 = 196 - 165 = 31 \pmod{33}$，$31 \cdot 14 = 434$，$434 = 33 \times 13 + 5$，故 $m = 5$。✅

---

## 八、知识链接

- [[求和式的计算方式]] — 莫比乌斯反演中的双重求和换序技巧
- [[整除与同余基础]] — 费马小定理与逆元、欧拉定理的前置知识
- [[二次剩余与阶]] — 从欧拉定理到阶与原根的进阶内容
- [[集合]] — 容斥原理与欧拉函数计算公式的推导
- [[复数]] — 单位根与莫比乌斯反演的类比（离散傅里叶变换思想）